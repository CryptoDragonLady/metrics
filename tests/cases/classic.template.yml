- name: 📗 Classic template - Example
  uses: CryptoDragonLady/metrics@latest
  with:
    token: MOCKED_TOKEN
    base: header, repositories
    plugin_lines: yes
    use_mocked_data: yes
    verify: yes
