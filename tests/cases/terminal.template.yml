- name: 📙 Terminal template - Example
  uses: CryptoDragonLady/metrics@latest
  with:
    template: terminal
    token: MOCKED_TOKEN
    base: header, metadata
    use_mocked_data: yes
    verify: yes
